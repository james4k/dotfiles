# unbind some default keybindings
unbind C-b

# set prefix key to ctrl-q
set -g prefix C-q

# pass through a ctrl-q if you press it twice
bind C-q send-prefix

# immediate escape key - we dont use any esc combos for tmux
set -sg escape-time 0

